{
  "name": "sassclase12",
  "version": "1.0.0",
  "description": "loquesea",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "node-sass": "node-sass --include-path scss sass/main.scss estilos/estilos.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""    
  },

  "author": "Juan Bikman",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^6.0.1",
    "nodemon": "^2.0.12"
  }
}
